# Mapbox Public Token (starts with pk.)
# Used in the app to render maps
EXPO_PUBLIC_MAPBOX_TOKEN=pk_XXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Mapbox Downloads Token (starts with sk.)
# Used only in EAS build environment for iOS builds
# Set this in EAS project secrets, not in local .env
MAPBOX_DOWNLOADS_TOKEN=sk_XXXXXXXXXXXXXXXXXXXXXXXXXXXX

